{{- /*
Validation for chart-facing values now lives in values.schema.json so Helm can fail
before template rendering with clearer type and required-value errors.

Keep this template reserved for chart-local checks that cannot be expressed cleanly
in JSON schema without causing duplicate or noisy failures.
*/ -}}
